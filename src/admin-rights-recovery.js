import { supabase } from './supabaseClient';
let viewer=null;
async function loadViewer(){if(viewer)return viewer;const {data:{user}}=await supabase.auth.getUser();if(!user)return null;const {data}=await supabase.from('profiles').select('id,role').eq('id',user.id).maybeSingle();viewer=data||null;return viewer}
function clickOldAdmin(){const old=[...document.querySelectorAll('button')].find(b=>/Admin-Zentrale/i.test(b.textContent||''));if(old){old.click();return true}return false}
async function recover(){const me=await loadViewer();if(!me||!['HEAD_ADMIN','ADMIN'].includes(String(me.role||'').toUpperCase()))return;const dock=document.querySelector('.ec-right-dock');if(!dock||dock.querySelector('.ec-dock-admin-recovery'))return;const box=document.createElement('div');box.className='ec-dock-admin-recovery';const divider=document.createElement('div');divider.className='ec-dock-divider';const label=document.createElement('div');label.className='ec-dock-section-label';label.textContent='VERWALTUNG';const admin=document.createElement('button');admin.type='button';admin.innerHTML='<b>⚙</b><span>Admin-Zentrale</span>';admin.onclick=()=>{if(!clickOldAdmin())window.dispatchEvent(new CustomEvent('ec:open-admin'))};box.append(divider,label,admin);if(String(me.role).toUpperCase()==='HEAD_ADMIN'){const regional=document.createElement('button');regional.type='button';regional.innerHTML='<b>⌖</b><span>Regionen & Rechte</span>';regional.onclick=()=>{if(!clickOldAdmin())window.dispatchEvent(new CustomEvent('ec:open-admin'));setTimeout(()=>{const el=[...document.querySelectorAll('button,h2,h3')].find(x=>/Region|Rolle|Rechte verwalten/i.test(x.textContent||''));el?.scrollIntoView({behavior:'smooth',block:'center'})},250)};box.appendChild(regional)}dock.appendChild(box)}
let busy=false;function schedule(){if(busy)return;busy=true;setTimeout(()=>{busy=false;recover()},80)}new MutationObserver(schedule).observe(document.documentElement,{subtree:true,childList:true});window.addEventListener('DOMContentLoaded',schedule);schedule();
