import { supabase } from './supabaseClient';

const THEMES={
  'layout-theme-red':{key:'red',logo:'/ennstal-connect-wordmark-red.svg'},
  'layout-theme-blue':{key:'blue',logo:'/ennstal-connect-wordmark-blue.svg'},
  'layout-theme-neon':{key:'neon',logo:'/ennstal-connect-wordmark.svg'},
};
const DEFAULT_LOGO='/ennstal-connect-wordmark.svg';
const LEGACY_LAYOUTS=new Set(['alpine','aurora','ocean','slate','ember','redwood','lavender','midnight','sunrise','neon']);
const LEVEL_FLOORS={Neu:0,Aktiv:10,Verbunden:30,Engagiert:75,'Community-Profi':150,Stammmitglied:300};
const PRESTIGE_FLOORS={Bronze:300,Silber:450,Gold:650,Platin:900};
const COMPONENT_LABELS={online:'Aktive Onlinezeit',forum_posts:'Forum-Beiträge',forum_replies:'Antworten',friendships:'Freundschaften',groups:'Gruppen',events:'Events',polls:'Umfragen',community_requests:'Community-Hilfe',profile:'Profil vollständig'};
let lastKey='';
let queued=false;
let progress=null;
let progressLoadedAt=0;
let progressPromise=null;
let savedLayout='standard';
let savedLayoutLoadedAt=0;
const withTimeout=(promise,ms=7000)=>Promise.race([promise,new Promise((_,reject)=>setTimeout(()=>reject(new Error('Theme-Daten Timeout')),ms))]);

function layoutOptions(state){const redOpen=Boolean(state?.red_unlocked),blueOpen=Boolean(state?.blue_unlocked);return[['standard','Standard – Ennstal Connect',true],['theme-red',redOpen?'Connect Rot – Hellrot':'🔒 Connect Rot – ab 30 Aktivitätspunkten',redOpen],['theme-blue',blueOpen?'Connect Blau – Kräftig':'🔒 Connect Blau – ab 150 Aktivitätspunkten',blueOpen],['theme-neon','Neon Grün – Giftgrün & Dunkel',true]]}
function normalizeLayoutSelect(select){if(!select)return;const current=String(select.value||'standard'),options=layoutOptions(progress),signature=options.map(([value,label,enabled])=>`${value}:${label}:${enabled}`).join('|');if(select.dataset.ecThemeSignature!==signature){select.replaceChildren(...options.map(([value,label,enabled])=>{const option=document.createElement('option');option.value=value;option.textContent=label;option.disabled=!enabled;return option}));select.dataset.ecThemeSignature=signature}const preferred=(savedLayout&&options.some(([value,,enabled])=>value===savedLayout&&enabled))?savedLayout:current;const wanted=options.some(([value,,enabled])=>value===preferred&&enabled)?preferred:'standard';if(select.value!==wanted)select.value=wanted}
function rewardText(state){if(!state)return'Aktivitätsfortschritt derzeit nicht verfügbar.';const score=Number(state.score||0);if(!state.red_unlocked)return`Noch ${Math.max(0,30-score)} Punkte bis Connect Rot.`;if(!state.blue_unlocked)return`Connect Rot ist frei. Noch ${Math.max(0,150-score)} Punkte bis Connect Blau.`;if(score<300)return`Rot und Blau sind frei. Noch ${300-score} Punkte bis Prestige Bronze.`;if(state.prestige_next_score==null)return'Prestige Platin erreicht. Deine Aktivität bleibt weiterhin sichtbar.';return`Prestige ${state.prestige}. Noch ${Math.max(0,Number(state.prestige_next_score)-score)} Punkte bis zur nächsten Prestige-Stufe.`}
function progressPercent(state){if(!state)return 0;const score=Number(state.score||0),next=state.next_score==null?null:Number(state.next_score);if(!next)return 100;const floor=Number(LEVEL_FLOORS[state.level]??0);return Math.max(0,Math.min(100,Math.round(((score-floor)/Math.max(1,next-floor))*100)))}
function prestigePercent(state){if(!state)return 0;const score=Number(state.score||0),next=state.prestige_next_score==null?null:Number(state.prestige_next_score);if(!next)return 100;const floor=Number(state.prestige?PRESTIGE_FLOORS[state.prestige]:300);return Math.max(0,Math.min(100,Math.round(((score-floor)/Math.max(1,next-floor))*100)))}
function renderProgress(section){if(!section)return;let card=section.querySelector('.ec-activity-progress');if(!card){card=document.createElement('div');card.className='ec-activity-progress';const select=section.querySelector('select[name="profile_layout"]');if(select)section.insertBefore(card,select);else section.appendChild(card)}if(!progress){card.innerHTML='<div class="ec-activity-progress-head"><span>COMMUNITY-LEVEL</span><strong>Derzeit nicht verfügbar</strong></div>';return}const score=Number(progress.score||0),next=progress.next_score==null?null:Number(progress.next_score),components=Object.entries(progress.components||{}).filter(([,points])=>Number(points||0)>0),prestigeLabel=progress.prestige?`Prestige ${progress.prestige}`:'Prestige ab 300 Punkten',prestigeNext=progress.prestige_next_score==null?'höchste Prestige-Stufe':`nächste Stufe bei ${Number(progress.prestige_next_score)}`;card.innerHTML=`<div class="ec-activity-progress-head"><span>COMMUNITY-LEVEL</span><strong>${String(progress.level||'Neu')}</strong><b>${score} Punkte${next?` · nächstes Level bei ${next}`:' · Level abgeschlossen'}</b></div><div class="ec-activity-progress-bar"><i style="width:${progressPercent(progress)}%"></i></div><div class="ec-activity-prestige-head"><span>${prestigeLabel}</span><b>${prestigeNext}</b></div><div class="ec-activity-progress-bar ec-activity-prestige-bar"><i style="width:${prestigePercent(progress)}%"></i></div><p>${rewardText(progress)}</p><div class="ec-activity-reward-chips"><span class="${progress.red_unlocked?'is-unlocked':'is-locked'}">${progress.red_unlocked?'✓':'🔒'} Connect Rot</span><span class="${progress.blue_unlocked?'is-unlocked':'is-locked'}">${progress.blue_unlocked?'✓':'🔒'} Connect Blau</span><span class="is-unlocked">✓ Neon Grün</span>${progress.prestige?`<span class="is-prestige">★ ${progress.prestige}</span>`:''}</div><details class="ec-activity-breakdown"><summary>Wie entstehen meine Punkte?</summary><p>Aktive Onlinezeit zählt langsam weiter. Beiträge, Antworten, Freundschaften, Gruppen, Events und regionale Community-Aktivität zählen stärker.</p><div>${components.map(([key,points])=>`<span><b>+${Number(points)}</b> ${COMPONENT_LABELS[key]||key}</span>`).join('')}</div></details>`}
function normalizeLayoutControls(){document.querySelectorAll('select[name="profile_layout"]').forEach(normalizeLayoutSelect);document.querySelectorAll('.layout-rewards').forEach(section=>{const heading=section.querySelector('h3'),copy=section.querySelector('p');if(heading)heading.textContent='Dein Layout';if(copy&&!copy.closest('.ec-activity-progress'))copy.textContent='Der Aufbau bleibt immer gleich. Standard, Rot, Blau und Neon sind voneinander getrennte Designs.';renderProgress(section)})}
async function loadSavedLayout(force=false){if(!supabase)return savedLayout;if(!force&&savedLayoutLoadedAt&&Date.now()-savedLayoutLoadedAt<120000)return savedLayout;try{const {data:{session}}=await withTimeout(supabase.auth.getSession(),5000);if(!session?.user)return savedLayout;const {data,error}=await withTimeout(supabase.from('profiles').select('profile_layout').eq('id',session.user.id).maybeSingle(),7000);if(!error&&data?.profile_layout)savedLayout=String(data.profile_layout);savedLayoutLoadedAt=Date.now()}catch(error){console.warn('Gespeichertes Layout konnte nicht geladen werden:',error?.message||error)}return savedLayout}
async function loadProgress(force=false){if(!supabase)return null;if(!force&&progress&&Date.now()-progressLoadedAt<120000)return progress;if(progressPromise)return progressPromise;progressPromise=(async()=>{try{await loadSavedLayout(force);const {data,error}=await withTimeout(supabase.rpc('community_activity_progress'),7000);if(error)throw error;progress=data||null;progressLoadedAt=Date.now();normalizeLayoutControls();queueSync();return progress}catch(error){console.warn('Community-Level konnte nicht geladen werden:',error?.message||error);normalizeLayoutControls();queueSync();return null}finally{progressPromise=null}})();return progressPromise}
function syncTheme(){normalizeLayoutControls();if(document.querySelector('select[name="profile_layout"]'))void loadProgress(false);const app=document.querySelector('.app');if(app)[...LEGACY_LAYOUTS].forEach(legacy=>app.classList.remove(`layout-${legacy}`));if(app){app.classList.remove('layout-theme-neon');if(savedLayout==='theme-neon')app.classList.add('layout-theme-neon')}const match=app?Object.entries(THEMES).find(([className])=>app.classList.contains(className)):null;const next=match?match[1]:{key:'standard',logo:DEFAULT_LOGO};if(next.key!==lastKey){document.documentElement.dataset.ecTheme=next.key;lastKey=next.key}document.querySelectorAll('.ec-brand-image').forEach(img=>{if(img.getAttribute('src')!==next.logo)img.setAttribute('src',next.logo)})}
function queueSync(){if(queued)return;queued=true;requestAnimationFrame(()=>{queued=false;syncTheme()})}
function refreshProgress(){progressLoadedAt=0;savedLayoutLoadedAt=0;void loadProgress(true);queueSync()}
function boot(){void loadSavedLayout(true).finally(()=>{syncTheme();setTimeout(queueSync,250);setTimeout(queueSync,900)});void loadProgress(false)}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();
window.addEventListener('ec:navigate',()=>{queueSync();if(document.querySelector('select[name="profile_layout"]'))void loadProgress(false)});
window.addEventListener('ec:region-change',queueSync);
window.addEventListener('focus',queueSync);
window.addEventListener('ec:activity-progress-refresh',refreshProgress);
window.addEventListener('ec:online-reward',refreshProgress);
