// Legacy member-profile role/region DOM polishing disabled.
// React owns the visible member profile and its role label. Region-specific
// presentation must not mutate the currently rendered profile after navigation.
export {};
