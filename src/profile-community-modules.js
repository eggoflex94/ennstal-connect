// Legacy profile DOM injection disabled.
// Member profile groups, personal sections and photo areas are rendered by React.
// Keeping this file as a no-op avoids old direct DOM mutations fighting React
// during profile switches and causing flicker or stale profile overlays.
export {};
