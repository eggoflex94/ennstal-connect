import { supabase } from "./supabaseClient";
import { roleIdentity,makeRoleIcon } from "./roleIdentity.js";
const SUPPORT_EMAIL="ennstal.connect@gmx.at";let refreshRunning=false,lastSignature="",realtimeStarted=false,pollTimer=null;const normalizeRole=v=>String(v||"").trim().toUpperCase(),displayName=p=>p.nickname||"Community-Team";
function findHostCard(){const h=[...document.querySelectorAll("h1,h2,h3,h4")].find(n=>/^Administration(?:\s*&\s*Support)?$/i.test(String(n.textContent||"").trim())||/^Ansprechpartner$/i.test(String(n.textContent||"").trim()));return h?.closest("section,article,.card,.dashboard-card,.home-card,.panel")||h?.parentElement||null}
function hideLegacyContacts(host,heading,panel){[...host.children].forEach(c=>{if(c===heading||c===panel||c.classList?.contains("eyebrow"))return;const t=String(c.textContent||"").trim();if(/Marco|Roland|Zuständig\s+für|Nachrichten verwalten|Profilbesuche|Community-Verwaltung|Technischen Support|Technischer Support|Hauptadmin|Betreiber|Moderation/i.test(t)){c.style.display="none";c.dataset.ecLegacyContact="true"}})}
function makePerson(p){const ident=roleIdentity(p),card=document.createElement("article");card.className=`ec-team-person ec-role-${ident.key}`;card.style.setProperty("--ec-role-color",ident.color);const top=document.createElement("button");top.type="button";top.className="ec-team-person-top";top.dataset.profileId=p.user_id||p.id||"";top.title=`Profil von ${displayName(p)} öffnen`;const star=makeRoleIcon(p,"normal");if(star)top.appendChild(star);const name=document.createElement("strong");name.className="ec-team-name ec-identity-name";name.textContent=displayName(p);top.appendChild(name);top.addEventListener("click",()=>window.dispatchEvent(new CustomEvent("ec:open-profile",{detail:{profileId:p.user_id||p.id,nickname:displayName(p)}})));card.appendChild(top);return card}
function renderTeam(profiles){const host=findHostCard();if(!host)return;const heading=[...host.querySelectorAll("h1,h2,h3,h4")].find(n=>/Administration|Ansprechpartner/i.test(n.textContent||""));if(heading)heading.textContent="Administration & Support";let panel=host.querySelector(".ec-team-panel");if(!panel){panel=document.createElement("section");panel.className="ec-team-panel";const old=host.querySelector(".ec-support-panel");if(old)old.replaceWith(panel);else heading?.insertAdjacentElement("afterend",panel)}hideLegacyContacts(host,heading,panel);panel.replaceChildren();const intro=document.createElement("div");intro.className="ec-team-intro";const copy=document.createElement("div");copy.innerHTML="<strong>Support & Ansprechpartner</strong><p>Die Supportbereiche und Verifizierung findest du gesammelt oben unter „Support“.</p>";const email=document.createElement("a");email.className="ec-team-email";email.href=`mailto:${SUPPORT_EMAIL}`;email.textContent="✉ E-Mail senden";intro.append(copy,email);panel.appendChild(intro);const groups=[["HEAD_ADMIN","Administration"],["ADMIN","Administration"],["SUPPORTER","Moderation"]];groups.forEach(([r,label])=>{let members=profiles.filter(p=>normalizeRole(p.role)===r);if(r==="SUPPORTER")members=members.filter(p=>p.forum_moderator||p.group_moderator);if(!members.length)return;const g=document.createElement("div");g.className="ec-team-group";const title=document.createElement("div");title.className="ec-team-group-title";title.textContent=label;const grid=document.createElement("div");grid.className="ec-team-grid";members.forEach(p=>grid.appendChild(makePerson(p)));g.append(title,grid);panel.appendChild(g)})}
function signature(ps){return ps.map(p=>[p.user_id,p.role,p.nickname,p.forum_moderator,p.group_moderator].join("|")).join("::")}
async function refreshTeam(){if(!supabase||refreshRunning||!findHostCard())return;refreshRunning=true;try{const{data,error}=await supabase.rpc("community_moderation_contacts");if(error)throw error;const profiles=(data||[]).map(p=>({...p,id:p.user_id}));const sig=signature(profiles);if(sig!==lastSignature||!document.querySelector(".ec-team-panel")){lastSignature=sig;renderTeam(profiles)}}catch(e){console.error("Administration & Support konnte nicht synchronisiert werden:",e)}finally{refreshRunning=false}}
function startRealtime(){if(!supabase||realtimeStarted)return;realtimeStarted=true;supabase.channel("ec-team-role-sync-v7").on("postgres_changes",{event:"*",schema:"public",table:"profiles"},()=>{lastSignature="";refreshTeam()}).on("postgres_changes",{event:"*",schema:"public",table:"user_permissions"},()=>{lastSignature="";refreshTeam()}).subscribe()}
function boot(){refreshTeam();startRealtime();if(!pollTimer)pollTimer=setInterval(()=>{if(!document.hidden)refreshTeam()},15000)}if(document.readyState==="loading")document.addEventListener("DOMContentLoaded",boot,{once:true});else boot();const observer=new MutationObserver(()=>{if(findHostCard()&&!document.querySelector(".ec-team-panel"))refreshTeam()});observer.observe(document.documentElement,{childList:true,subtree:true});