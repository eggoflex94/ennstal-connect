import './temporary-supporter-admin.js';

// Global homepage sections are rendered by homepage-global-runtime.js.
// This module intentionally keeps only the temporary supporter bootstrap so
// global homepage entries are not rendered a second time.
export {};
